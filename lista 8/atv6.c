#include <stdio.h>

int main() {
int codigo, i, contA, contB, contC, contD, contE;
float nota, somaA, somaB, somaC, somaD, somaE;

for (i = 1; i <= 4; i++){
    do{
   printf("Digite o Codigo do aluno");
    scanf("%d", &codigo); 

   printf("Digite a Nota do aluno");
    scanf("%f", &nota);
       }while (nota < 0 || nota > 10);
     if(nota >= 0 && nota <= 2.9){
        contE++
        somaE +=
     }
}







return 0;}
